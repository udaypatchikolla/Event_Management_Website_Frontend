import { FaWallet } from "react-icons/fa";

export default function WalletIcon({ size = 20, color = "currentColor", className = "" }) {
  return <FaWallet size={size} color={color} className={className} />;
}
